{
  "name": "tradearena",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently --restart-tries 10 --restart-after 2000 --names server,client --prefix-colors cyan,magenta \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client",
    "start": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
